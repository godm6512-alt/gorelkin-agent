
## bonus-gateway 401 — инцидент (разбор 2026-07-05)
Бонусники не могут скачать пакет. Корень: GITHUB_BONUS_TOKEN на Vercel
(проект jarvis-architect-bonus prj_dREpdUuNfhADdGp8UCNT4ybqvMLM) протух →
GitHub 401 → gateway upstream_error 502/401. Подтверждено логами (/api/admin/events):
16× github_401 на разные токены. Валидный токен на тот же zipball даёт 200 — репо/ветка ок.
Фикс: новый fine-grained PAT (Contents:read, repo jarvis-architect-bonus) → обновить
env на Vercel → REDEPLOY (RED, ждёт ✔). Часть людей (403 zip_invalid_token) — протухшие
install-токены, им /new ПОСЛЕ фикса. Разбор: Agent-factory-course/.../bonus-gateway-401-incident-2026-07-05.md.
Побочно: латентный баг обновления ботов v2.1.0 (update-bot.sh) — отдельно, jarvis-architect/knowledge/v2.1.0-update-failure-rootcause.md.

## bonus-gateway 401 — ЗАКРЫТО (2026-07-05, вечер)
Новый fine-grained PAT (GITHUB_BONUS_TOKEN_NEW, pull к jarvis-architect-bonus) записан
в GITHUB_BONUS_TOKEN на Vercel (env id ZV3kQPsnr7KlYvjW, все 3 окружения). Проверка decrypt: MATCH.
Redeploy прод dpl_9TFrpJXx → READY. Сквозной тест: точная копия upstream-запроса gateway
(zipball/main + User-Agent bonus-gateway) → 200, настоящий ZIP 4.2МБ с файлами репо.
Живой /api/zip на dummy-токен → 403 (функция поднялась с новым env). Путь скачивания рабочий.
Застрявшим (zip_invalid_token = протухшие install-токены) выдан текст: команда /new за свежей ссылкой.
Замечание на будущее: KV_REST_API_* на Vercel имеют type=sensitive → decrypt через API пустой,
поэтому замайнить тестовый токен прямо в KV нельзя; сквозной тест шёл через реплей upstream-запроса.

## bonus-gateway — ЖИВОЕ подтверждение (2026-07-05)
Дмитрий прислал /new в @jarchitect_bot → токен ...a0c1. Прогнал реальную ссылку промпта
https://jarvis-architect-bonus.vercel.app/zip/0ff40ac1-...-a0c1 → HTTP 200, ZIP 4.2МБ (PK),
файлы репо внутри. Последний стык (деплойнутый /zip → github fetch) закрыт вживую, не логикой.
Бот бонуса = @jarchitect_bot. Команда для застрявших = /new (лимит 3 скачивания на токен;
не-whitelist получает «доступ закрыт»). Инцидент закрыт полностью.

## Broadcast v2.1.0 — старый текст СЛОМАН, собран новый (2026-07-05)
Старое сообщение обновления (советовал ранее сам) вело в тупик: «/update в чате»
и фолбэк «bash /home/agent/.agent/bot/update-bot.sh» оба гоняют СТАРЫЙ on-disk
update-bot.sh (без claude-oauth.js/env-write.js) → ERR_MODULE_NOT_FOUND → автооткат.
Проверено по main index.js: /update handler (стр.1888-1899) качает свежий скрипт
только if(!existsSync), иначе запускает on-disk. update-guide.md сценарии A/B/C — те же
сломанные пути, не линковать.
Единственный рабочий путь v2.0→v2.1.0: curl свежий скрипт в /tmp → bash /tmp/update-bot.sh.
Токен не нужен (репо публичный), память не трогается (пишет только в .agent/bot/,
не в workspace/ и не в .agent/memory.db). Финальные тексты (установка с нуля + обновление)
в knowledge/messages-install-vs-update.md.
