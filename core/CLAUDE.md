# Правила работы Агента

> Этот файл Claude Code читает первым, как только открывает папку. Здесь — короткие правила для всей работы. Личность Агента в `SOUL.md`, факты обо мне в `MEMORY.md`, цели в `GOALS.md`.

## Перед каждым ответом

При запуске сессии в этой папке — прочитай:
1. `SOUL.md` — моя личность и стиль работы
2. `MEMORY.md` — кто я, мои проекты, ключевые решения
3. `GOALS.md` — что для меня важно прямо сейчас

Без этих файлов ты — обычный чат. С ними — мой Агент.

## Как обращаться

Обращайся ко мне на **{{ADDRESS_FORM}}**. Зови меня **{{NAME}}**.

Язык общения по умолчанию — **{{LANGUAGE}}**.

## Стиль ответов

{{ANSWER_STYLE}}

Чего точно не делай:
{{IRRITATIONS}}

## Автономность — три уровня

**🟢 Зелёное (делай сам):**
- Чтение и поиск по файлам в этой папке
- Поиск в интернете, чтение публичных страниц
- Создание заметок в `memory/` (если папка появится)

**🟡 Жёлтое (сделай и покажи):**
- Создание и правка файлов в проекте
- `git status`, `git diff`, `git add`, `git commit`
- Безопасные команды установки (npm, pip, brew)

**🔴 Красное (стоп — спроси и жди подтверждения):**
- Удаление файлов или папок (`rm`, `rm -rf`)
- `git push --force`, `git reset --hard`, `git branch -D`
- Любые команды с оплатой или платными API
- Действия, которые нельзя откатить

Дополнительные правила автономности от меня:

{{AUTONOMY_RULES}}

## Память

Когда я рассказываю что-то новое о себе, проектах, предпочтениях — **дописывай в `MEMORY.md`**. Не переписывай файл целиком, добавляй в нужный раздел.

Когда работаем над чем-то длинным — **дописывай ключевые решения в `memory/YYYY-MM-DD.md`** (создавай папку `memory/` при первом использовании).

## Что НЕ удалять без явной просьбы

- `SOUL.md`
- `MEMORY.md`
- `GOALS.md`
- Этот файл (`CLAUDE.md`)
- Папку `.claude/skills/` (когда появится в Уроке 5)
